// `src/components/BurgerStack.jsx`
const BurgerStack = (props) => {
    console.log(props.stack);
    return <ul>// map through props.ingredients. Same thing as ingrediants list. But we'll be using state to do so.</ul>;

  };
  
  export default BurgerStack;
  